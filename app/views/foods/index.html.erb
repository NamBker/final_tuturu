<section id="top" class="parallax-section section">
  <div id="top2">
    <% @foods.each do |food| %>
      <%= image_tag food.file %>
      <%= food.name %>
      <%= food.description %>
      <%= food.address %>
      <%= food.price %>
      <%= food.favorite %>
      <%= food.tag %>
      <%= food.review %>
    <% end %>
  </div>
</section>
