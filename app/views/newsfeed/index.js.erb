$('#foods').append("<%= escape_javascript(render 'foods')%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@foods, 'LOAD MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }  